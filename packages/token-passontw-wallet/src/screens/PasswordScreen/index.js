import { connect } from 'react-redux';
import PasswordScreen from './view';

const mapStateToProps = ({}) => ({});

const mapDispatchToProps = (dispatch) => ({});

export default connect(mapStateToProps, mapDispatchToProps)(PasswordScreen);
