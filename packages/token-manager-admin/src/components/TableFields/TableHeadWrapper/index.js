import styled from 'styled-components';
import { TableHead as BasicTableHead } from '@material-ui/core';

const TableHeadWrapper = styled(BasicTableHead)`
  position: relative;
}
`;

export default TableHeadWrapper;
