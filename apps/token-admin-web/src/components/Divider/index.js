import React from 'react';
import { Divider as BasicDivider, Box } from '@material-ui/core';

const Divider = () => {
  return (
    <Box pt={1}>
      <BasicDivider />
    </Box>
  );
};

export default Divider;
