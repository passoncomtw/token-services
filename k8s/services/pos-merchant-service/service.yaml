apiVersion: v1
kind: Service
metadata:
  name: pos-merchant-service
  labels:
    app: pos-merchant-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: pos-merchant-service
  sessionAffinity: None

