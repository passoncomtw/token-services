apiVersion: v1
kind: Service
metadata:
  name: pos-backend-service
  labels:
    app: pos-backend-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: pos-backend-service
  sessionAffinity: None

