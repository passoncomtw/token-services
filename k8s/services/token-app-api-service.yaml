apiVersion: v1
kind: Service
metadata:
  name: token-app-api
  labels:
    app: token-app-api
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: token-app-api
  sessionAffinity: None

