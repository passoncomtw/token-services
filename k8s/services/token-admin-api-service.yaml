apiVersion: v1
kind: Service
metadata:
  name: token-admin-api
  namespace: passontw-services
  labels:
    app: token-admin-api
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: token-admin-api
  sessionAffinity: None

