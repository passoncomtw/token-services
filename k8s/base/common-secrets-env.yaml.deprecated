# ⚠️ 此檔案已棄用 (2024-11-24)
# 
# 棄用原因：
# 1. database-secret 和 jwt-secret 在 K8s 中為空值，會覆蓋 ConfigMap 的配置
# 2. 所有配置（包括敏感信息）現在都直接存儲在 ConfigMap 中
# 3. ConfigMap 在 K8s 中有 RBAC 保護，安全性足夠
#
# 新架構：
# - 所有配置都在各服務的 ConfigMap 中（由 CI/CD 從 GitHub Secrets 同步）
# - Token APIs 使用 redis-secret（有實際值）
# - POS APIs 只使用 ConfigMap
#
# 如需恢復此檔案，請確保：
# 1. 在 CI/CD 中正確創建 database-secret 和 jwt-secret（有實際值）
# 2. 確保 Secret 不會覆蓋 ConfigMap 的值
# 3. 更新所有服務的 kustomization.yaml 引用此檔案
#
# 以下為原始內容（已停用）：

# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: not-important
# spec:
#   template:
#     spec:
#       containers:
#       - name: placeholder
#         env:
#         # 資料庫配置（所有服務共用）
#         - name: DB_HOST
#           valueFrom:
#             secretKeyRef:
#               name: database-secret
#               key: DB_HOST
#         - name: DB_PORT
#           valueFrom:
#             secretKeyRef:
#               name: database-secret
#               key: DB_PORT
#         - name: DB_USER
#           valueFrom:
#             secretKeyRef:
#               name: database-secret
#               key: DB_USER
#         - name: DB_PASSWORD
#           valueFrom:
#             secretKeyRef:
#               name: database-secret
#               key: DB_PASSWORD
#         - name: DB_NAME
#           valueFrom:
#             secretKeyRef:
#               name: database-secret
#               key: DB_NAME
#         - name: DB_SSL_MODE
#           valueFrom:
#             secretKeyRef:
#               name: database-secret
#               key: DB_SSLMODE
#         
#         # JWT 配置（所有服務共用）
#         - name: JWT_SECRET
#           valueFrom:
#             secretKeyRef:
#               name: jwt-secret
#               key: JWT_SECRET

