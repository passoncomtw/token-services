apiVersion: v1
kind: Secret
metadata:
  name: jwt-secret
  # 注意：部署時需要使用 -n 參數指定命名空間
  # 例如：kubectl apply -f jwt-secret.yaml -n passontw-services-staging
type: Opaque
stringData:
  # 請將以下值替換為實際的 JWT 密鑰（至少 32 字符）
  JWT_SECRET: "your-super-secret-jwt-key-at-least-32-chars-long"

